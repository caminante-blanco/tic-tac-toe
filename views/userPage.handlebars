<!DOCTYPE html>
<html lang="en">
<head>
    <title>TIC-TAC-TOE - {{user.username}}</title>
    <link rel="stylesheet" href="/style.css">
    <meta charset="UTF-8">
</head>
<body>
    <div class="container">
        <div class="title">TIC-TAC-TOE</div>
        <div class="sections">
            <div class="accountInfo">
                <div class="title">Account</div>
                <div class="text">
                    <div id="accountDetails">
                        <div id="accountStats">
                            <p id="welcomeMessage">Welcome, {{user.username}}!</p>
                            <p id="statsMessage">Wins: {{user.wins}}, Losses: {{user.losses}}, Draws: {{user.cats}}</p>
                        </div>
                        <input type="hidden" id="playerUUID" value="{{user.uuid}}">
                        <label for="aiGame">Play against AI:</label>
                        <input type="checkbox" id="aiGame">
                        <button id="newGameButton">New Game</button>
                    </div>
                </div>
            </div>
            <div class="grid">
                grid
            </div>
            <div class="leaderboard">
                <div class="title">Ladder</div>
                Leaderboard
            </div>
        </div>
    </div>
    <script src="/loginScript.js"></script>
</body>
</html>
